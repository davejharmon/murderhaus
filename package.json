{
  "name": "murderhaus",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"wait-on tcp:8080 && npm run client\"",
    "devflow": "node devflow.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "open": "^10.2.0",
    "wait-on": "^9.0.1"
  }
}
